-- ============================================================================
-- tenge.gg — что уже применено к базе (миграция site_write_policies_reviews_sellers_realtime)
-- Этот файл — запись о сделанном. Повторно запускать не нужно.
-- Дата: 31.07.2026
-- ============================================================================
--
-- ИСПРАВЛЕНИЕ ПРЕДЫДУЩЕГО ВЫВОДА:
--   Ранее я написал, что любой может переписать цену лота. Это оказалось
--   НЕВЕРНО. Политика ls_owner_update = (seller_id = auth.uid() OR is_staff())
--   работает корректно, а ответ 204 на мой тест пришёл просто потому, что
--   фильтр не совпал ни с одной строкой (PostgREST так отвечает всегда).
--   Цены защищены и были защищены.
--
-- ЧТО БЫЛО РЕАЛЬНО СЛОМАНО И ПОЧИНЕНО:
--
-- 1) У listings не было ни одной INSERT-политики и гранта INSERT.
--    => sell.html молча не мог опубликовать лот. Добавлено:
--       grant insert to authenticated + policy ls_insert_own (seller_id = auth.uid())
--       + seller_id по умолчанию auth.uid()
--
-- 2) У orders не было ни INSERT, ни UPDATE политики.
--    => кнопка «Купить» молча ничего не создавала, таблица orders была пуста.
--       Добавлено: or_insert_buyer (buyer_id = auth.uid())
--                  or_update_party (покупатель / продавец / арбитр)
--
-- 3) Цену сделки задавал браузер.
--    => лот за 78 000 ₸ можно было «купить» за 1 ₸.
--       Триггер fill_order_from_listing теперь берёт цену и комиссию 5%
--       из самой таблицы listings, а поля из запроса игнорируются.
--
-- 4) Статусы сделки мог менять кто угодно из участников на любые.
--    => триггер enforce_order_transition: продавец может только
--       DATA_TRANSFERRED, покупатель только COMPLETED или DISPUTE,
--       суммы и участники после создания неизменяемы, арбитр может всё.
--
-- 5) Публикация supabase_realtime была ПУСТОЙ.
--    => лента сделок на главной и чат сделки никогда бы не обновились.
--       Добавлены таблицы orders и trade_messages.
--
-- 6) У анонима были лишние гранты (UPDATE/DELETE/TRUNCATE) на listings,
--    early_leads, user_roles, trade_messages. Отозваны — аноним теперь
--    только читает каталог и оставляет заявку.
--
-- 7) Созданы таблицы public.sellers (ник, галочка, число сделок)
--    и public.reviews (отзыв только по своей завершённой сделке, один на заказ).
--
-- ============================================================================
-- ОСТАЛОСЬ СДЕЛАТЬ РУКАМИ (один раз):
-- ============================================================================
--
-- Выдать себе роль арбитра, чтобы открылась admin.html.
-- Функция is_staff() принимает роли 'admin' и 'arbiter'.
--
--   insert into public.user_roles (user_id, role)
--   select id, 'admin' from auth.users where email = 'ТВОЙ_EMAIL'
--   on conflict (user_id) do update set role = 'admin';
--
-- Проверить, что всё на месте:
--
--   select tablename, policyname, cmd from pg_policies
--   where schemaname='public' and tablename in ('listings','orders','reviews','sellers')
--   order by tablename, cmd;
